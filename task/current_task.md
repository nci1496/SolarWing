# 当前任务卡片

## 任务名称
最终缺口检查 / 提交前验收

## 所属阶段
Phase 7：参数保存读取与验收

## 当前状态
- 主窗口已支持“保存配置 / 打开配置”
- 已生成 `StepTest.ini` 与 `SinTest.ini` 两个示例场景文件
- PID 参数交互与基本设置实际生效问题已修复
- 当前工程已基本具备课程演示与验收条件

## 提交前必须检查
1. 在程序中分别打开 `StepTest.ini` 与 `SinTest.ini`，确认曲线和动画能正常刷新
2. 确认“基本设置”中的背景色、文字色、字体、积分器切换均可见且可复现
3. 确认 PID 默认值显示为 `100 / 25 / 45`
4. 确认播放开始、暂停、停止三个动作行为正常
5. 确认达到最高位移时停止钩子工作正常

## 当前已知说明
- 当前 `SinTest.ini` 是基于现有“二次目标”能力组织的连续变化示例，并非真正正弦函数目标
- 如果老师或验收要求必须出现严格意义上的正弦目标，需要后续扩展 `TargetType`

## 建议录屏展示顺序
1. 打开程序展示三张曲线图与动画区
2. 打开 `StepTest.ini`，展示阶跃目标下的响应
3. 打开 `SinTest.ini`，展示连续变化目标下的响应
4. 修改基本设置中的字体 / 颜色 / 积分器，展示设置真实生效
5. 修改 PID 参数并重新播放，展示参数可调

## 如还有时间可增强
- 状态栏显示当前时间、实际位移、目标位移、误差
- 动画区增加实时数值标签
- 播放结束后的状态按钮联动优化
- 若课程要求严格正弦目标，新增 `Sinusoidal` 目标类型

## 后续接力提示
下一个 AI 开始前，请先判断：
1. 现在是直接进入最终联调，还是继续补“真·正弦目标”
2. 是否需要顺手补录屏脚本或报告提纲
3. 是否要做一次 build / run 级别的最终验收
