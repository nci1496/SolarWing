# 当前任务卡片

## 任务名称
进入课程验收与成果整理

## 所属阶段
Phase 7：参数保存读取与验收（最终验收）

## 当前背景
- `StepTest.ini` 与 `SinTest.ini` 已可打开并复现实验场景
- `StepTest` 当前效果基本满意
- `SinTest` 目前仍基于"二次目标"能力组织，暂不强行扩展为严格正弦目标
- 右侧动画区已完成信息增强与比例映射修正，可读性与演示体验达到验收要求
- 课程要求的参数保存 / 读取闭环已经走通，演示配置可直接复现

## 当前任务目标
- 围绕课程验收要求完成最终成果整理与状态归位
- 确认未完成或留作扩展的项已明确记录在任务文档
- 为后续可能的录屏、报告与现场演示提供稳定基线

## 本轮优先要做
1. 通览 `StepTest.ini` / `SinTest.ini` 复现场景，确认行为符合预期
2. 复核积分器切换（Euler / RK2 / RK4）在 `Limit` 改大时是否仍能稳定播放
3. 复核动画区四项实时数值、时间显示、刻度尺与拖拽进度条的表现
4. 复核播放完成后的状态恢复与状态栏提示

## 当前明确不做
- 暂不新增真正的 `Sinusoidal` / 正弦目标类型
- 暂不重做整体界面风格
- 暂不扩展与本轮无关的仿真模型能力
- 暂不补状态栏联动显示当前时间与误差（已记录在"留作扩展项"）

## 当前问题定义
- 当前最大缺口已不再是功能短板，而是"是否还有未验收的细节"
- 需以验收视角重新检查：开始 / 暂停 / 停止、上限报警、动画区联动、参数保存与复现是否全部联通

## 建议实现方向
1. 直接进入人工验收，按"保存配置 → 关闭程序 → 打开配置 → 播放 → 触发上限"流程走一遍
2. 验收过程中如发现非预期行为，再回到具体模块迭代
3. 验收完成后，将本轮成果与留作扩展项固定到任务文档

## 验收标准
- "设置"菜单中可正常"保存配置 / 打开配置"
- 打开 `StepTest.ini` / `SinTest.ini` 后，曲线、动画、对话框参数与保存时一致
- 播放流程中：`t=当前时间 / 总时长` 实时变化，进度条可拖拽跳转
- 动画区四项数值（`Target/Actual/Limit/Error`）实时刷新
- `Limit` 从小到大变化时，动画比例与刻度尺自适配，不出现"顶满"问题
- 触达上限时，报警对话框出现且仿真自动停止

## 留作扩展项（不阻塞验收）
- 状态栏联动显示当前播放时间与误差
- 真正的 `Sinusoidal` / 正弦目标类型
- 运行中参数修改的统一保护策略
- 播放结束后 `m_currentSampleIndex` 与进度条的最终位置回放

## 后续接力提示
下一个 AI 开始前，请先确认：
1. 课程验收是否还有未被现有任务覆盖的评分点
2. 是否需要把"留作扩展项"中的某一项升级为正式任务
3. 是否需要补一份给课程录屏使用的演示脚本
